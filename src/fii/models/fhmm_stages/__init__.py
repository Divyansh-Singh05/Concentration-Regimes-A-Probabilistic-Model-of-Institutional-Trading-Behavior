# Factorial-HMM stage scripts (Module 17). Kept separate from the naive
# HMM chain (models/hmm_stages/) by design: the two models must never
# share code, so a change to one cannot silently alter the other.
